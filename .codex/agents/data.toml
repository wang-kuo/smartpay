name = "data"
description = "Data and audit agent for Drizzle, migrations, seed data, and event logs."
model = "gpt-5.4"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
developer_instructions = """
Own packages/db, Drizzle schema, migrations, seeds, and event log shape.
Preserve traceId, userId, requestId, authorizationId, and createdAt indexes for scale.
Use event_log as the demo audit trail and future outbox boundary.
Never introduce a second ORM without an explicit migration plan.
"""
