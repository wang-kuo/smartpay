# Feature Plan Template

## Goal

Describe the user outcome and the SmartPay product behavior being changed.

## Success Criteria

- Final consumption decisions still resolve to `allow`, `ask`, or `deny`.
- Authorization -> decision -> execution remains the core flow.
- LLM output is structured context only and never direct authorization.

## Contracts

- API endpoints:
- Request schemas:
- Response schemas:
- Error schemas:
- Idempotency requirements:

## Data And Events

- Tables or fixtures:
- Event log entries:
- Trace propagation:
- Redaction behavior:

## Tests

- Unit:
- Integration:
- E2E:
- Security or release guard:

## Out Of Scope

List real integrations, payments, banks, or production rollout items that are not included.
